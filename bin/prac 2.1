using System;
using System.Collections.Generic;
using System.Linq;

class Programm
{
    static void Main(string[] args)
    {
        /* 1 
        Console.Write("Введите J: ");
        string J = Console.ReadLine();
        Console.Write("Введите S: ");
        string S = Console.ReadLine();
        int count = 0;
        for (int i = 0; i < J.Length; i++)
        {
            if (S.Contains(J[i]))
            {
                count++;
            }
        }
        Console.WriteLine(count);*/

        /* 2 задание 
        List<int> candidates = new List<int> {8, 7, 2, 5, 3, 1};
        int target = 10;

        show(candidates, target);
    }

        static void show(List<int> candidates, int target)
        {
            for (int i = 0; i < candidates.Count; i++)
            {
                for (int j = 0; j < candidates.Count; j++)
                {
                    if (candidates[i] + candidates[j] == target) // если сумма найдена
                    {
                        Console.WriteLine("Пара найдена (" + candidates[i] + ", " + candidates[j] + ")");
                        return;
                    }
                }
            }
            Console.WriteLine("Пара не найдена");
        }*/

        /* 3 zad
        List<int> array = new List<int> {1, 2, 3, 4};
        bool cont = false;
        for (int i = 0; i < array.Count; i++)
        {
            for (int j = 0; j < array.Count; j++)
            {
                if (i != j)
                {
                    if(array[i] == array[j])
                    {
                        cont = true;
                        break;
                    }
                }
            }
        }
        Console.WriteLine(cont);*/
    }
}
